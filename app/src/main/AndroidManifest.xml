<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== Core telephony / dialer permissions ===== -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <!-- Lets the incoming-call service turn the screen on for a ringing call while the phone is asleep -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Firebase Auth (Google Sign-In) + Firestore cloud backup / video-call signaling -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Video calling (WebRTC) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <!--
        Android 11+ (API 30) package visibility: without declaring this, an
        Intent targeted with setPackage("com.whatsapp") can silently fail to
        resolve even when WhatsApp is installed, forcing the WhatsApp quick
        action (Recents/Contacts long-press menu, contact detail) to always
        fall back to the plain wa.me browser link instead of opening the app
        directly.
    -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="org.telegram.messenger" />
        <package android:name="org.telegram.messenger.web" />
        <package android:name="org.thunderdog.challegram" />
        <package android:name="com.instagram.android" />
        <package android:name="moe.shizuku.manager" />
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <application
        android:name=".AshuDialerApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AshuDialer"
        tools:targetApi="34">

        <!-- ===== Main UI Activity ===== -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.AshuDialer">
            <!-- Required so the system offers this app when user taps "Set as default dialer" targets -->
            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.DIAL" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="tel" />
            </intent-filter>
        </activity>

        <!--
            "Phone" launcher icon - opens MainActivity straight into the Dialer / Recent Calls
            tab (see MainActivity.launcherTabFor, called from onCreate and onNewIntent, which
            reads the LAUNCHER_TAB meta-data below to pick DialerTab.RECENT). Carries the
            MAIN/LAUNCHER filter that used to sit directly on MainActivity, so this alias - not
            MainActivity itself - is now the Home-screen icon; MainActivity's DIAL filters above
            still make it the target the system launches for "set as default dialer" and tel:
            links, independent of which launcher icon exists.
        -->
        <activity-alias
            android:name=".LauncherPhone"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:label="@string/app_name_launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="com.ashudialer.app.LAUNCHER_TAB" android:value="RECENT" />
        </activity-alias>

        <!--
            "Contacts" launcher icon - opens MainActivity straight into the Contacts tab. Same
            MainActivity, same task (android:launchMode="singleTask" above means a second icon
            never opens a second window), just a different LAUNCHER_TAB meta-data value below.
        -->
        <activity-alias
            android:name=".LauncherContacts"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_contacts"
            android:label="@string/app_name_contacts">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="com.ashudialer.app.LAUNCHER_TAB" android:value="CONTACTS" />
        </activity-alias>

        <!-- ===== Full-screen in-call UI ===== -->
        <activity
            android:name=".telecom.InCallActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:theme="@style/Theme.AshuDialer.Call" />

        <!-- ===== WebRTC video call UI (separate from the regular Telecom-backed InCallActivity, since video calls are app-to-app over the internet rather than carrier calls) ===== -->
        <activity
            android:name=".telecom.VideoCallActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:showOnLockScreen="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:configChanges="screenSize|smallestScreenSize|screenLayout|orientation|density|uiMode"
            android:theme="@style/Theme.AshuDialer.Call" />

        <!-- ===== Required: InCallService — lets this app draw the call screen ===== -->
        <service
            android:name=".telecom.PixelInCallService"
            android:permission="android.permission.BIND_INCALL_SERVICE"
            android:exported="true"
            android:foregroundServiceType="phoneCall">
            <meta-data
                android:name="android.telecom.IN_CALL_SERVICE_UI"
                android:value="true" />
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
        </service>

        <!-- ===== Required: CallScreeningService — spam/blocked-number filtering ===== -->
        <service
            android:name=".telecom.PixelCallScreeningService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- ===== Background listener for incoming (callee-side) video calls — see VideoCallListenerService's doc comment ===== -->
        <service
            android:name=".telecom.VideoCallListenerService"
            android:exported="false"
            android:foregroundServiceType="remoteMessaging" />

        <!-- ===== Required: default DIAL intent handler for outgoing calls ===== -->
        <activity
            android:name=".telecom.OutgoingCallActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.CALL" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="tel" />
            </intent-filter>
        </activity>

        <!-- ===== Handles Answer/Decline taps directly from the incoming-call notification ===== -->
        <receiver
            android:name=".telecom.CallActionReceiver"
            android:exported="false" />

        <!-- ===== Fires callback reminders scheduled from Recents, and handles their "Call now"/"Dismiss" notification actions ===== -->
        <receiver
            android:name=".telecom.CallbackReminderReceiver"
            android:exported="false" />

        <!-- ===== Re-schedules pending callback reminders after a reboot, since AlarmManager alarms don't survive one ===== -->
        <receiver
            android:name=".telecom.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>


        <!-- ===== Lets a recording file be shared via Intent.ACTION_SEND without exposing a raw file:// URI ===== -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.ashudialer.app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
