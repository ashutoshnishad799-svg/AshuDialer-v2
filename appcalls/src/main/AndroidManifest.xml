<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Permissions the call recorder needs. They are merged into the app's manifest.
        Each is requested at runtime from the Call recording setup screen, in this order.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Legacy storage (Android 9 and below) so recordings can be written to Music/. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Lets the app see whether Shizuku (or Sui) is installed - Android 11+ package visibility. -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <queries>
        <package android:name="moe.shizuku.privileged.api" />
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <package android:name="org.telegram.messenger" />
        <package android:name="org.telegram.messenger.web" />
    </queries>

    <application>

        <!-- Records the call. type=specialUse on Android 14+, dataSync below that (chosen in code). -->
        <service
            android:name="com.ashudialer.app.appcalls.recording.RecordingForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync|specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Records phone and app calls the user chose to record, using Shizuku" />
        </service>

        <!-- The system tells us about every call state change here, no matter which dialer placed the call. -->
        <receiver
            android:name="com.ashudialer.app.appcalls.recording.PhoneStateReceiver"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>
        </receiver>

        <!-- Delete button on the "Recording saved" notification. -->
        <receiver
            android:name="com.ashudialer.app.appcalls.recording.PostRecordingActionReceiver"
            android:exported="false" />

        <!-- Shizuku hands its binder to this provider so Shizuku.pingBinder() works. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!--
            WhatsApp / Telegram call detection. Requires the user to grant Settings >
            Notification access. The permission and action must match exactly or Android will
            not bind this service.
        -->
        <service
            android:name="com.ashudialer.app.appcalls.AppCallNotificationListenerService"
            android:label="@string/appcalls_notification_listener_label"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
